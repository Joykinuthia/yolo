apiVersion: apps/v1
kind: Deployment
metadata:
  name: yolo-frontend
  namespace: default
  labels:
    app: yolo
    tier: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: yolo
      tier: frontend
  template:
    metadata:
      labels:
        app: yolo
        tier: frontend